<!-- -*- coding: utf-8-unix; mode: markdown; -*- -->
# ユーザーへの情報

本インストラクションの目的は

- AI に `追加の方針` を与え、既存の CSS ファイルをリファインする
- 既存の CSS ファイルを そのファイル内の基準で正規化する.
  ファイル間の正規化は @REFACTOR.md で行うので
  本インストラクションの立ち入るところではない.

# 用語の定義

- まず、@TERM.md で共通の用語を確認する

- `REFINEDS`:
  本インストラクションでリファインするファイル名のリスト

- `REFINED`:
  `REFINEDS` の要素で
  本インストラクションでリファインするファイル名

# 大まかな作業

1. 本インストラクションは `(REFINE REFINED ...)` の形式で
   呼び出される. `REFINED ...` の中にはワイルドカード表現が含まれる
   こともあり、それらを展開してリスト `REFINEDS` にする
2. `REFINEDS` の要素がゼロ個なら、その旨を表示して終了する
3. `REFINEDS` の各要素 `REFINED` は全て既存の書き込み可能ファイルの
   ファイル名である. 存在しないファイルのファイル名、または
   書き込み不可能なファイルのファイル名を指定したときは、
   それを指摘し終了
4. `REFINEDS` の各要素 `REFINED` は全て CSS ファイルでなければならない.
   CSS ファイルでないものがあるときは、それを指摘し終了する.
5. `REFINEDS` を表示
6. `REFINEDS` の各要素 `REFINED` で、以下の作業をループする
   1. そのファイル名のファイルの内容を以下の詳細に基づきリファインし
      そのファイルと同じフォルダ内の TEST.css に書き込む.
      同名のファイルがあるときは上書きせよ.
   2. 変更箇所があったときは、前後の diff を表示
   3. 変更箇所がなかったときは、その旨を表示し次の要素へ continue
   4. `方針` `追加の方針` `提案` を表示
   5. 下記 `制約` 3 に反する場合は その旨も表示せよ
   6. 書き換えるか確認する.
      - 書き換え確認で b なら
        - `REFINED` へ書き込みしないので、今回の `提案` は履歴に残さない
        - ループを break する
      - 書き換え確認で c なら
        - `REFINED` へ書き込みしないので、今回の `提案` は履歴に残さない
        - ループを continue する
      - 書き換え確認で y なら `REFINED` を書き換えて次の要素へ continue
      - 書き換え確認で w なら
        - 「新しいファイル名を入力せよ」と表示し、ユーザーからの入力を待つ
        - ユーザー入力ファイル名が *.css なファイル名でないときは
          書き換え確認に戻る
        - ユーザー入力ファイル名が *.css なファイル名のとき
          - ユーザー入力ファイル名にフォルダー区切り文字がなければ
            `REFINED` と同じフォルダー内で、
            ユーザー入力ファイル名のファイルに書き込む.
            元のファイルはそのまま残す. 次の要素へ
          - ユーザー入力ファイル名にフォルダー区切り文字があれば
            ワークスペースルートからのパスとし、
            ユーザー入力ファイル名のファイルに書き込む.
            元のファイルはそのまま残す. 次の要素へ
      - 書き換え確認で r なら
        - `REFINED` へ書き込みしないので、今回の `提案` は履歴に残さない
        - ループを redo する
      - 書き換え確認で R なら
        - 現在の `追加の方針` を表示
        - `追加の方針` の入力をユーザーに促す
          - ユーザーの入力が空文字列や空白だけの文字列なら
            追加の方針を `なし` とする
          - ユーザーの入力を `追加の方針` に設定する
        - `REFINED` へ書き込みしないので、今回の `提案` は履歴に残さない
        - ループを redo する.

# リファインの詳細

以下のコードブロックをテンプレートとして記述する.
`冒頭のコメントブロック` の `タイトル` `タイトルの和訳`
`方針` `追加の方針` `履歴のリスト` を更新せよ.

```
{`エディタ用先頭コメントブロック`}

{`冒頭のコメントブロック`}

{`ルールセットのリスト`}

{`エディタ用末尾コメントブロック`}
```

- `REFINED` に `エディタ用先頭コメントブロック` がない、
  または @TERM.md と異なるときは @TERM.md の定義に従う.

- `REFINED` に `エディタ用末尾コメントブロック` がない、
  または @TERM.md と異なるときは @TERM.md の定義に従う.

- `REFINED` に `冒頭のコメントブロック` がないとき @TERM.md の定義に従う.

- `REFINED` の `冒頭のコメントブロック` が @TERM.md の定義と異なるとき、
  `REFINED` の `冒頭のコメントブロック` は以下のいずれかの形式と見なし
  対応するものを `前回の提案` とせよ. 空行は 1行以上の空行も含む

| `REFINED` の `冒頭のコメントブロック`                             | `前回の提案`     |
|:------------------------------------------------------------------|------------------|
| `タイトル`                                                        | `タイトル`       |
| `タイトルの和訳`                                                  | `タイトルの和訳` |
| `提案`                                                            | `提案`           |
| `タイトル` + 空行 + `提案`                                        | `提案`           |
| `タイトル` + 空行 + `提案` + 空行 + ...                           | `提案`           |
| `タイトルの和訳` + 空行 + `提案`                                  | `提案`           |
| `タイトルの和訳` + 空行 + `提案` + 空行 + ...                     | `提案`           |
| `タイトル` + 空行 + `タイトルの和訳` + 空行 + `提案`              | `提案`           |
| `タイトル` + 空行 + `タイトルの和訳` + 空行 + `提案` + 空行 + ... | `提案`           |
| 上記のいずれでもない                                              | 最初のパラグラフ |

- `方針` または `追加の方針` が `なし` のときは下記の `制約` に基づいた
  `ルールセット` のリファクタリングのみ行う. つまり宣言の値を変更しない.
  この場合 `提案` は `なし` とする

- 下記の `制約` に基づき `方針` と `追加の方針` に沿った
  `提案` を作り `ルールセットのリスト` をリファインする.

# `制約`

1. shorthand プロパティを使用せず、shorthand でないプロパティで宣言せよ.
   例えば、background なら アルファベット順に
   background-attachment background-clip
   background-color background-image
   background-origin background-position-x background-position-y
   background-repeat background-size に分割せよ.
   shorthand で指定されなかったプロパティの値はデフォルト値で補完せよ
2. REFINED にないセレクタリストを新設してはならないが、
   REFINED にあるセレクタリストに疑似要素を加えたものは
   新設して良い.
3. shorthand 展開で増えたプロパティを除き、REFINED にないプロパティを
   使用しないことを推奨する. ただし `大まかな作業` 6.5 で表示し、
   その後の書き換え確認でユーザーが判断するので、これは努力目標とする

<!-- Local Variables: -->
<!-- indent-tabs-mode: nil -->
<!-- End: -->
