<!-- -*- coding: utf-8-unix; mode: markdown; -*- -->
# 用語の定義

- `ルールセット`:
  セレクタリストと宣言ブロックを合わせた ひとかたまりのこと

- `ルールセットのリスト`:
  ファイル冒頭や末尾のコメントを含まない `ルールセット` だけのリスト.

- `エディタ用先頭コメントブロック`:
  文字コードや言語モードをエディタに知らせるためのコメントブロック.
  存在するなら、1行目か2行目にある.
  Emacs ではローカル変数行（Local Variables Line）と呼ぶ. 例えば
  - CSS なら
    `/* -*- coding: utf-8-unix; mode: css-ts; -*- */`
  - Markdown なら
    `<!-- -*- coding: utf-8-unix; mode: markdown; -*- -->`

- `エディタ用末尾コメントブロック`:
  ファイルの末尾にあるインデントなどの書式の情報をエディタに
  知らせるためのコードブロック.
  Emacs ではコレもローカル変数行と呼ぶ. 例えば

  - CSS なら
```
/* Local Variables: */
/* indent-tabs-mode: nil */
/* css-indent-offset: 2 */
/* End: */
```

  - Markdown なら
```
<!-- Local Variables: -->
<!-- indent-tabs-mode: nil -->
<!-- End: -->
```

- `冒頭のコメントブロック`:
  `エディタ用先頭コメントブロック`があれば 2番目のコメントブロック.
  `エディタ用先頭コメントブロック`がなければ最初のコメントブロック.
  以下のコードブロックをテンプレートとする.

```
/*
{`タイトル`}

{`タイトルの和訳`}

{`提案`}

- 方針: {`方針`}
- 追加: {`追加の方針`}
{`履歴のリスト`}
 */
```

- `方針`:
  リファインのための大前提. 値は以下の優先順位で決まる
  1. ユーザープロンプトで明示的に指定されたなら、その文言。
  2. `冒頭のコメントブロック` に `方針` の記述があるなら、その文言.
  3. 対象とするファイルと同じフォルダに INSTRUCTION.md があり、
     見出しまたは項目として `方針` の記述があるなら、その文言.
  4. 上記のいずれにも `方針` がないときは `なし`

- `追加の方針`:
  `方針` の追加情報. 値は以下の優先順位で決まる
  1. ユーザープロンプトで明示的に指定されたなら、その文言.
  2. `冒頭のコメントブロック` に `追加の方針` の記述があるなら、その文言. 
  3. 対象とするファイルと同じフォルダに INSTRUCTION.md があり、
     見出しまたは項目として `追加の方針` の記述があるなら、その文言.
  4. 上記のいずれにも `追加の方針` がないときは `なし`

- `提案`:
  `方針` と `追加の方針` に沿って AI が具体化したデザインの意図.
  1行または複数行だが、ひとつのパラグラフなので空行を含まない.
  リファクタリングのみのときは変更しない.

- `変更年月日`:
  YYYY-mm-dd HH:MM:SS 形式とする

- `前回の提案`:
  リファインする前の `提案`.
  新規作成であれば `新規作成`

- `履歴`:
  リファインや新規作成時の `変更年月日` と `前回の提案` の ひと組.
  文字列としては "- {`変更年月日`}: {`前回の提案`}" になる

- `履歴のリスト`:
  全ての `履歴` のリスト. 最新の `履歴` が最初になる

- `タイトル`:
  `冒頭のコメントブロック` で意味のある最初の 1行のみを指す.
  最新の `提案` を英語で空白を含め 68文字以下で要約.
  リファインのたびに新しい `提案` の要約に置き換わる.
  リファクタリングのみのときは変更しない.

- `タイトルの和訳`:
  `タイトル` を和訳したもので全角 34文字以下の 1行とする

<!-- Local Variables: -->
<!-- indent-tabs-mode: nil -->
<!-- End: -->
